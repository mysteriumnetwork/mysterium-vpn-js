{
  "name": "mysterium-vpn-js",
  "version": "0.0.1",
  "description": "Library for domain logic used in MysteriumVPN applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn tsc && ./utils/generate_flow_definitions.sh",
    "ci": "yarn tsc-check && yarn lint && yarn test",
    "tsc-check": "yarn tsc --noEmit",
    "lint": "tslint -p .",
    "test": "jest"
  },
  "author": "Mysterium Network <team@netsys.technology>",
  "license": "GPL-3.0",
  "private": false,
  "devDependencies": {
    "@types/jest": "<24.0.0",
    "flowgen": "^1.5.7",
    "jest": "<24.0.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.3.1"
  }
}
